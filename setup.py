
from setuptools import setup, find_packages

setup(name='usf_oncho',
      version='0.0.1',
      description='Uses computer vision to quell the spread of Onchocerciasis',
      packages=['usf_oncho'])
